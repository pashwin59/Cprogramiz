#include <stdio.h>
#include <string.h>

int main() {
    char s[] = "sss";
  
    // Finding and printing length of string s
    printf("The length of the string is %lu\n",  strlen(s));
    return 0;
}