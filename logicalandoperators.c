#include <stdio.h>
int main()
{
    int a = -12, b = -1;

    if (a > 0 || b > 0){

        printf("Both values are greater than 0\n");
    }
    else
    {
        printf("Both values are less than 0\n");
    }
    return 0;
}